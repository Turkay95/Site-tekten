 
<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2">Tableau de bord</h1>
</div>

<div class="row">
  <div class="col-lg-6">
    <div class="card mb-4">
      <div class="card-header d-flex justify-content-between align-items-center">
        <strong>Paramètres d'affichage du site</strong>
        <div>
          <a href="/" class="btn btn-sm btn-outline-secondary me-2" target="_blank">Voir Accueil</a>
          <a href="/boutique" class="btn btn-sm btn-outline-primary me-2" target="_blank">Voir Boutique</a>
          <a href="/configurateur" class="btn btn-sm btn-outline-success" target="_blank">Voir Configurateur</a>
        </div>
      </div>
      <div class="card-body">
        <form method="POST" action="/admin/settings">
          <div class="form-check form-switch mb-3">
            <input class="form-check-input" type="checkbox" role="switch" id="shopFromDb" name="shopFromDb" <%= (settings && settings.shopFromDb) ? 'checked' : '' %>>
            <label class="form-check-label" for="shopFromDb">Boutique alimentée par la base de données</label>
          </div>
          <div class="form-check form-switch mb-3">
            <input class="form-check-input" type="checkbox" role="switch" id="configFromDb" name="configFromDb" <%= (settings && settings.configFromDb) ? 'checked' : '' %>>
            <label class="form-check-label" for="configFromDb">Configurateur alimenté par la base de données</label>
          </div>
          <button type="submit" class="btn btn-dark">Enregistrer</button>
        </form>
        <small class="text-muted d-block mt-3">Astuce: Publiez les produits/composants (case "Publié") pour qu'ils apparaissent sur le site.</small>
      </div>
    </div>
  </div>
</div>

<div class="row">
    <div class="col-md-6">
        <div class="card mb-4">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h5 class="card-title">Produits</h5>
                        <p class="card-text display-6"><%= productsCount || 0 %></p>
                    </div>
                    <i class="bi bi-bicycle" style="font-size: 3rem; color: #0d6efd;"></i>
                </div>
                <a href="/admin/products" class="btn btn-outline-primary mt-3">Gérer les produits</a>
            </div>
        </div>
    </div>
    <div class="col-md-6">
        <div class="card mb-4">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h5 class="card-title">Composants</h5>
                        <p class="card-text display-6"><%= componentsCount || 0 %></p>
                    </div>
                    <i class="bi bi-gear" style="font-size: 3rem; color: #198754;"></i>
                </div>
                <a href="/admin/components" class="btn btn-outline-success mt-3">Gérer les composants</a>
            </div>
        </div>
    </div>
</div>

<div class="card mt-4">
    <div class="card-header">
        <h5 class="mb-0">Derniers produits ajoutés</h5>
    </div>
    <div class="card-body">
        <% if (recentProducts && recentProducts.length > 0) { %>
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Image</th>
                            <th>Nom</th>
                            <th>Catégorie</th>
                            <th>Prix</th>
                            <th>Stock</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% recentProducts.forEach(product => { %>
                            <tr>
                                <td>
                                    <% if (product.image) { %>
                                        <img src="<%= product.image %>" alt="<%= product.name %>" style="width: 50px; height: 50px; object-fit: cover;">
                                    <% } else { %>
                                        <div class="bg-light d-flex align-items-center justify-content-center" style="width: 50px; height: 50px;">
                                            <i class="bi bi-image text-muted"></i>
                                        </div>
                                    <% } %>
                                </td>
                                <td><%= product.name %></td>
                                <td><span class="badge bg-primary"><%= product.category %></span></td>
                                <td><%= product.price.toFixed(2) %> €</td>
                                <td>
                                    <span class="badge <%= product.stock > 0 ? 'bg-success' : 'bg-danger' %>">
                                        <%= product.stock %> en stock
                                    </span>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
            <div class="text-end">
                <a href="/admin/products" class="btn btn-sm btn-outline-primary">Voir tous les produits</a>
            </div>
        <% } else { %>
            <div class="text-center py-4">
                <p class="text-muted">Aucun produit n'a été ajouté pour le moment.</p>
                <a href="/admin/products/add" class="btn btn-primary">Ajouter un produit</a>
            </div>
        <% } %>
    </div>
</div>
